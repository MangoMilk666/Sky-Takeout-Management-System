# IntelliDining 智能外送平台（管理端 Web）

本目录为“苍穹外卖”管理端前端的 React 版本改编工程（Vite + React + TypeScript）。

## 开发

- 安装依赖：`npm i`
- 启动开发：`npm run dev`

## 环境变量

- `VITE_BASE_API`：API 前缀，默认 `/api`
- `VITE_PROXY_TARGET`：开发代理目标，默认 `http://localhost:8080/admin`

